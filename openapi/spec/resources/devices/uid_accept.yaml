put:
  operationId: acceptDevice
  summary: Accept device
  description: Change device status to `accepted`.
  tags:
    - device
  security:
    - Auth: []
  parameters:
    - $ref: parameter/uid.yaml
  responses:
    '200':
      description: Success to accept the device
    '401':
      $ref: ../../shared/responses/unauthorized.yaml
    '500':
      $ref: ../../shared/responses/any_internal_error.yaml
