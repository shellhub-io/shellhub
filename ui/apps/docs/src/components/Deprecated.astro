---
interface Props {
  /** Overrides the word, for a marker that is not literally "Deprecated". */
  label?: string;
}

const { label = "Deprecated" } = Astro.props;
---

<span class="badge-deprecated">{label}</span>
