// Cross-app links. Hardcoded to local dev URLs for now: the website has no
// production build/deploy yet, so dev is the only environment it runs in.
// Production URLs (and any env-driven config) are a follow-up pending team input.
export const consoleUrl = "http://localhost";
export const docsUrl = "http://docs.localhost";
export const githubUrl = "https://github.com/shellhub-io/shellhub";
export const loginUrl = `${consoleUrl}/login`;
export const signupUrl = `${consoleUrl}/sign-up`;
