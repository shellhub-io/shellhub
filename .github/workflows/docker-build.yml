name: docker-build

on:
  push:
    branches: [master]
    paths:
      - "**Dockerfile"

  pull_request_target:
    branches: [master]
    paths:
      - "**Dockerfile"

jobs:
  build:
    name: Build '${{ matrix.project }}' Docker container

    strategy:
      fail-fast: false
      matrix:
        project: [api, ssh, gateway, ui, cli]

    runs-on: ubuntu-24.04
    environment: ${{ github.event.pull_request.head.repo.fork && 'fork-ci' || '' }}

    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Login to Docker Registry
        uses: docker/login-action@v4
        with:
          registry: registry.infra.ossystems.io
          username: ${{ secrets.PORTUS_USERNAME }}
          password: ${{ secrets.PORTUS_TOKEN }}

      - name: Build '${{ matrix.project }}' Docker container
        uses: docker/build-push-action@v7
        with:
          tags: shellhubio/${{ matrix.project }}:latest
          push: false
          file: ${{ matrix.project }}/Dockerfile

